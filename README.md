# mbl_scarper
mbl.is Article Scraper — A Python tool that searches mbl.is by keyword and scrapes matching article content (title, date, body text) using Playwright. Handles Cloudflare protection by running a visible browser, and resumes across sessions by tracking already-scraped URLs.
